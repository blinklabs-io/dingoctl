name: golangci-lint

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - 'v*'

jobs:
  golangci-lint:
    uses: blinklabs-io/actions/.github/workflows/reuseable-golangci-lint.yml@main
    with:
      go-version: 1.25.x
